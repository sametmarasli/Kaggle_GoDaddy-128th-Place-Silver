# revise the pipeline in a way that i can change the target value:
    - try ratio of mdensity
    - try only modeling active rather than mdensity, since mdensity is a function of both active and population
